# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtualenvs
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/

# Training outputs (regenerated by the runners)
phase1_checkpoints/
phase1_training_output/
phase1_modal_checkpoints/
phase2_tiny_smoke/
phase2_output/
phase2_parallel_output/
checkpoint_output/

# phase1-uniform-init-output/ is committed (per-target eps_star, viz,
# metadata). Only the 1.2 GB replay_buffer.pkl is excluded — that artifact
# lives on the Modal Volume `cs224r-phase1-uniform-init-buffer`. Match both
# the legacy root location and the current dynamic_beam_steering/ location.
phase1-uniform-init-output/replay_buffer.pkl
dynamic_beam_steering/phase1-uniform-init-output/replay_buffer.pkl

# Logs / scratch
*.log
runs/
wandb/

# LaTeX build intermediates (keep .tex/.sty/.bib/.bbl/.pdf)
*.aux
*.fls
*.fdb_latexmk
*.out
*.blg
*.synctex.gz
